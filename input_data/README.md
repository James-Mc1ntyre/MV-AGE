# Input Data

Add your project CSV files to this folder before running `prepare`.

Required files:

- `ingredients.csv`
- `nutrients.csv`
- `initial_labels.csv`
- `food_names.csv`

Optional file:

- `full_labels.csv`

CSV files in this folder are ignored by Git so local datasets are not committed by accident.
